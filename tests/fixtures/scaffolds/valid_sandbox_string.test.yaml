name: test-with-sandbox-reference
files:
  - path: docs/policies/example.md
executor:
  agent: doc-validator
  user_prompt: "Validate the policy document."
  tools:
    - read_file
judges:
  - name: accuracy
    system_prompt: "Evaluate the executor's work."
sandbox: default-sandbox
